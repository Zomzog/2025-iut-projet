meta {
  name: Create error missing address
  type: http
  seq: 40
}

post {
  url: http://localhost:8081/api/v1/peoples
  body: json
  auth: inherit
}

headers {
  X-User: pony
}

body:json {
  {
    "firstName": "John",
    "lastName": "Doe",
    "age": 30
  }
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
